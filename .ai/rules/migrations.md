---
paths:
  - 'database/migrations/**'
---

# Migrations

## All Schema Changes Through Migrations
Create or modify database tables, columns, indexes, constraints, and enum-limited values only through Laravel migrations. Do not apply ad hoc schema SQL outside migrations.
